# Copie este arquivo para .streamlit/secrets.toml e preencha os valores.
# O arquivo secrets.toml NÃO deve ser commitado (já está no .gitignore).
# No Streamlit Cloud, configure esses valores em Settings → Secrets.

GEMINI_API_KEY = "sua-chave-gemini-aqui"
LOGIN_USER = "admin"
LOGIN_PASSWORD = "troque-por-senha-forte"
